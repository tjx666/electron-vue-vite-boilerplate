{
    "groupRules": [
        ["^[@][^/]", {}],
        [
            "^@(/|$)",
            "^apis?(/|$)",
            "^assets(/|$)",
            "^components?(/|$)",
            "^pages?(/|$)",
            "^store(/|$)",
            "^typings?(/|$)",
            "^utils(/|$)"
        ],
        { "flags": "named", "regex": "^[.]" },
        [{ "flags": "scripts" }, { "flags": "scripts", "regex": "[.]((css)|(less)|(scss))$" }]
    ],
    "wrappingStyle": "prettier"
}
